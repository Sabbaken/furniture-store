export const fetchSection = (teacherID) => {
  return (dispatch, getState) => {
    console.log('fetchSection action')
    // dispatch({type: 'LOAD_TEACHER_DATA_SUCCESS', payload: {teacher: {...doc.data(), teacherID}}})
  }
}
